{
  "name": "tslkit-monorepo",
  "version": "0.1.0",
  "private": true,
  "description": "TSLKit WebGPU engine-first workspace",
  "license": "MIT",
  "workspaces": [
    "packages/*",
    "apps/*"
  ],
  "scripts": {
    "build": "npm run build --workspaces --if-present",
    "test": "npm run test --workspaces --if-present",
    "lint": "npm run lint --workspaces --if-present"
  },
  "engines": {
    "node": ">=18.18.0"
  }
}
